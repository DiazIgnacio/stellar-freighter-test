import { Response } from "./types";
declare global {
    interface Window {
        freighter: boolean;
    }
}
export declare const sendMessageToContentScript: (msg: {}) => Promise<Response>;
export declare const sendMessageToBackground: (msg: {}) => Promise<Response>;
